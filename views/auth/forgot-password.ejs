<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 100%;
            max-width: 400px;
            margin: 50px auto;
            background-color: white;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
            border-top: 10px solid #800000;
            text-align: center;
            border-radius: 4px;
        }

        header {
            background-color: #4e2b7f;
            color: white;
            padding: 20px 0;
        }

        header img {
            width: 60px;
            margin-bottom: 10px;
        }

        header h1 {
            font-size: 18px;
            margin: 5px 0;
            color: #f1c40f;
            font-weight: normal;
        }

        header p {
            font-size: 14px;
            margin: 0;
            color: #f4f4f4;
        }

        .form-container {
            padding: 22px;
        }

        .form-container h2 {
            font-size: 18px;
            margin-bottom: 22px;
            color: #4e2b7f;
            font-weight: normal;
        }

        form label {
            display: block;
            text-align: left;
            font-size: 14px;
            color: #333;
            margin-top:8px ;
            margin-bottom: 8px;
        }

        form input[type="text"],
        form input[type="email"],
        form input[type="password"] {
            width: calc(100% - 22px);
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        form button {
            width: 100%;
            padding: 10px;
            background-color: #4e2b7f;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        form button:hover {
            background-color: #3b2160;
        }

        a {
            display: inline-block;
            margin: 10px 5px;
            color: #4e2b7f;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }
        #loading-screen {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background: rgba(255, 255, 255, 0.8);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}

.spinner {
  border: 16px solid #f3f3f3;
  border-top: 16px solid #3498db;
  border-radius: 50%;
  width: 120px;
  height: 120px;
  animation: spin 2s linear infinite;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

    </style>
</head>
<body>
    <div class="container">
        <header>
            <img src="https://www.biyanitechnologies.com/img/gecr16a.jpg" alt="Logo">
            <h1>Public Grievances Portal</h1>
            <p>Online Grievances Redressal System</p>
        </header>
        
        <div class="form-container">
            <h2>Forgot Password</h2>
            <form action="#" method="POST" id="forgot-password-form">
                <label for="email">Registered Email Address *</label>
                <input type="email" id="email" name="email" required>
                
                <button type="submit">Generate OTP</button>
            </form>
            
            <div id="otp-field" style="display: none;">
                <form action="#" method="POST" id="otp-form">
                    <label for="otp">Enter OTP *</label>
                    <input type="text" id="otp" name="otp" required>
                    <label for="newPassword">New Password</label>
                    <input type="password" id="newPassword" name="newPassword" required>
                    <button type="submit">Verify OTP</button>
                </form>
            </div>
        </div>
    </div>
    <div id="loading-screen" style="display:none;">
        <div class="spinner"></div> <!-- Replace with your spinner/loader -->
        <p>Loading, please wait...</p>
      </div>

   <script src="/js/forgot-password.js"></script>
</body>
</html>